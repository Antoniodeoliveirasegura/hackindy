# pnpm: use a flat node_modules (no symlinks) so installs work on Windows
# without developer-mode/symlink permissions. CI (Linux) is unaffected.
node-linker=hoisted

# Don't auto-install when running scripts; CI installs explicitly.
verify-deps-before-run=false

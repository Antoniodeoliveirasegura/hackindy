# pnpm 10+ reads settings from here, not .npmrc.
# Flat node_modules (no symlinks) so installs work on exFAT/Windows drives
# without symlink permissions. CI (Linux) is unaffected by the linker choice.
nodeLinker: hoisted
